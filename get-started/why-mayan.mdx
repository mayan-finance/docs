---
title: "Why Mayan"
description: "What Mayan gives you that a fixed route does not."
---

**Instant settlement.** Swaps complete in seconds, not minutes. Whether someone is swapping on swap.mayan.finance or inside a wallet running on Mayan, the money moves when they ask for it.

**Competitive pricing.** Prices are competed for rather than quoted. The market bids on every order in a [real-time auction](/architecture/auction), so the price is the best the market can produce at that moment.

**Any token, any chain.** Any token with a contract address, across every chain Mayan supports. Nothing to whitelist, nothing to add token by token.

**One-click simplicity.** A cross-chain swap feels the same as a same-chain swap. One signature, and everything else happens underneath.

**Non-custodial.** Nobody takes custody of a user's assets at any point.

**Routes for every trade.** Three swap methods sit behind Mayan, [Swift](/architecture/swift-v2), [MCTP](/architecture/mctp) and [Wormhole Swap](/architecture/wh-swap), so a move can be optimized for what it actually needs, whether that is low slippage on a large transfer or instant settlement on a fast one.

**One integration.** Wherever a user needs to go, whatever token they need to hold, and whatever they need to do onchain once they arrive, it runs through the same [integration](/build/sdk).

**Direct execution.** Going direct to Mayan cuts out the middle layer. Users get the price at the source, and the [fee](/build/fees-earning) on every swap is yours.

## Start integrating

Integrate in a day. The Quickstart runs a live cross-chain swap in five minutes.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/get-started/quickstart">
    Your first cross-chain swap in five minutes.
  </Card>

  <Card title="SDK" icon="cube" href="/build/sdk">
    The primary integration path for cross-chain swaps.
  </Card>
</CardGroup>