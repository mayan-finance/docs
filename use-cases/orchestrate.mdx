---
title: "Orchestrate"
description: "Attach any onchain action to a single swap."
---

Getting a user into a vault from another chain normally means moving funds, swapping, switching apps, then depositing. Orchestrate collapses that into one action.

## How it works

A small message travels with the transfer and arrives with it. The user sets one intent covering the amount, the destination, and what should happen on arrival. Mayan moves the funds and the instruction together, and the action runs as the funds land.

What you can attach:

- **Deposit.** Funds arrive and go straight into a protocol or vault.
- **Stake.** Assets are staked on arrival, so users start earning without a second step.
- **Mint, join or participate.** The transfer triggers it as the funds land.
- **Your own logic.** Encoded contract calls, IDs, session data, or routing information your app needs.

## Requirements

Orchestrate needs a custom relayer and a destination contract on your side. It is the one part of Mayan that is not a drop-in, which is worth knowing before you scope it. See [Zaps](/integration/custom-payload).

## Examples

- A vault turns a multi-step entry into one click.
- A yield app puts deposits to work the moment they land.
- A trading app funds a position and opens it in the same action.

## Get started

<CardGroup cols={2}>
  <Card title="Zaps" icon="bolt" href="/integration/custom-payload">
    Custom payloads, relayer setup and destination contract requirements.
  </Card>

  <Card title="SDK" icon="cube" href="/build/sdk">
    The full surface: quotes, execution and every supported chain.
  </Card>
</CardGroup>